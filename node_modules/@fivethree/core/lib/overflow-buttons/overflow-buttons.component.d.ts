import { Platform, PopoverController, ActionSheetController } from '@ionic/angular';
import { OnInit, QueryList, AfterContentInit } from '@angular/core';
import { FivButton } from '../button/button.component';
export declare class FivOverflowButtons implements OnInit, AfterContentInit {
    private platform;
    private popoverController;
    actionSheetController: ActionSheetController;
    slot: string;
    icon: string;
    text: string;
    mode: 'popover' | 'action-sheet' | 'bottom-sheet';
    color: string;
    count: number;
    buttons: QueryList<FivButton>;
    constructor(platform: Platform, popoverController: PopoverController, actionSheetController: ActionSheetController);
    ngOnInit(): void;
    ngAfterContentInit(): void;
    onMenuClicked(event: any): Promise<void>;
    presentPopover(ev: any): Promise<void>;
    presentActionSheet(ev: any): Promise<void>;
    onButtonClicked(i: number): void;
}
